
import styled from 'styled-components'

const Container = styled.div`
    padding: 10px;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
`

const Profucts = ({ cat, filters }) => {

    return (
        <Container>
            
        </Container>
    )
}

export default Profucts
